<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">
    <item
        android:id="@+id/favorite"
        android:icon="@drawable/ic_menu_favorite"
        android:title="@string/title_menu_favorite"
        app:showAsAction="ifRoom"/>

    <item
        android:id="@+id/watch_later"
        android:title="@string/title_menu_watch_later"
        android:icon="@drawable/ic_menu_watch_later"
        app:showAsAction="ifRoom"/>

    <item
        android:id="@+id/selections"
        android:title="@string/title_menu_selections"
        android:icon="@drawable/ic_menu_selections"
        app:showAsAction="ifRoom"/>

    <item
        android:id="@+id/home"
        android:title="@string/title_menu_home"
        android:icon="@drawable/ic_menu_home"
        app:showAsAction="ifRoom"/>
</menu>